{% extends "base.html.twig" %}

{% block body %}
	<style>
		body {
			background-color: #222;
			color: white;
		}
	</style>

    <h1 class="display-4 text-center my-4"> Création d'un nouveleau membre </h1>


    {{ form_start(formmembre,                {'attr': {'class': 'col-md-4 mx-auto'}                                                           }) }}
        {{ form_row(formmembre.email,        {'attr': {'placeholder': "Email"},               'label': "Email"      }) }}
        {{ form_row(formmembre.username,     {'attr': {'placeholder': "Nom d'utilisateur"},             'label': "Nom d'utilisateur"    }) }}
        {{ form_row(formmembre.password,     {'attr': {'placeholder': "Mot de passe"},             'label': "Mot de passe"    }) }}
        {{ form_row(formArticle.roles,   {                                                             'label': "Catégorie"               }) }}     
        <button type="submit" class="btn btn-primary">
        {% if editMode %}   Enregistrer les modification
        {% else %}          Ajouter une membre
        {% endif %}</button>
    {{ form_end(formmembre) }}


{% endblock %}